Nothing to configure. The module extends `customization.operation` with the
QWeb view type and the report anchor kind; access rights, bundles and the
export wizard stay the ones `web_customizer` installs.

Only members of *Customization / Manager* can write or apply operations.
